VITE_API_URL="http://localhost:8002"
VITE_API_PREFIX="/api/v1"
GROQ_API_KEY="your_groq_key_here"
VITE_AWS_PUBLIC_URL="https://cdn.threadzip.com"

# Keep false/empty for normal local dev so Vite proxies /api/v1 to the backend.
# Set true only when you want the browser to call VITE_API_URL directly.
VITE_USE_DIRECT_API="false"
